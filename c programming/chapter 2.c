// operators and operator precedence
// instructions are statements in a program
// type declaration = declare var before using it, it says to declare a variable before assigning any value to it
// arithmetic instructions= arithmetic instructions are used to allow arithmetic operations
// control instructions= they are their to guide the operations from here to there and generally they control the syntax
#include<stdio.h>
int main(){
    int oldAge=33;
    int newAge=343;
    int years =2;
    int sum=oldAge+years;
    printf("%d",sum);
    
    return 0;
}